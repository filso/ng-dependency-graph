'use strict';

angular.module('ngDependencyGraph')
  .factory('controls', function(chart) {

    /**
     * Add:
     * group by module / ungroup
     * checkboxes:
     * - modules list
     * - component types list
     *  
     */

    // 


    var service = {
      setUppercaseIsModel: function(val) {

      },
      filterNodesByType: function(typesList) {
        // controller / directive etc

      },
      filterModulesByName: function(list) {

      },

    };

    return service;
  });