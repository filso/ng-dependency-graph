'use strict';

describe('inject', function() {

  describe('is initialised correctly', function() {

    it('with angular.bootstrap', function() {
      // console.log(inject);
      injectCode();
    });

    it('with angular.bootstrap', function() {
      // console.log(inject);
      injectCode();
    });

    xit('when angular.bootstrap when more then 1 app is present', function() {
      // it should just ignore subsequent angular.bootstrap calls
    });

  });

});